<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE manifest>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.tailriver.agoraguide"
    android:versionCode="4"
    android:versionName="1.9" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AgoraGuide" >
        <activity
            android:name="AgoraGuideActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:permission="android.permission.INTERNET" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="CreditsActivity"
            android:label="@string/credits" >
        </activity>
        <activity
            android:name="EntryDetailActivity"
            android:launchMode="singleTop" >
        </activity>
        <activity
            android:name="FavoritesActivity"
            android:label="@string/favorites" >
        </activity>
        <activity android:name="MapActivity" >
        </activity>
        <activity
            android:name="SearchByKeywordActivity"
            android:label="@string/searchByKeyword" >
        </activity>
        <activity
            android:name="SearchByScheduleActivity"
            android:label="@string/searchBySchedule" >
        </activity>

        <receiver
            android:name="ScheduleAlarm"
            android:process=":remote" >
        </receiver>
    </application>

</manifest>